---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: agentpools.app.terraform.io
spec:
  group: app.terraform.io
  names:
    kind: AgentPool
    listKind: AgentPoolList
    plural: agentpools
    singular: agentpool
  scope: Namespaced
  versions:
  - name: v1alpha2
    schema:
      openAPIV3Schema:
        description: AgentPool is the Schema for the agentpools API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AgentPoolSpec defines the desired state of AgentPool.
            properties:
              agentTokens:
                description: List of the agent tokens to generate.
                items:
                  description: 'Agent Token is a secret token that a Terraform Cloud
                    Agent is used to connect to the Terraform Cloud Agent Pool. More
                    infromation: - https://developer.hashicorp.com/terraform/cloud-docs/agents'
                  properties:
                    createdAt:
                      description: Timestamp of when the agent token was created.
                      format: int64
                      type: integer
                    id:
                      description: Agent Token ID.
                      type: string
                    lastUsedAt:
                      description: Timestamp of when the agent token was last used.
                      format: int64
                      type: integer
                    name:
                      description: Agent Token name.
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
              name:
                description: 'Agent Pool name: More information: - https://developer.hashicorp.com/terraform/cloud-docs/agents/agent-pools'
                type: string
              organization:
                description: 'Organization name where the Workspace will be created.
                  More information: - https://www.terraform.io/cloud-docs/users-teams-organizations/organizations'
                type: string
              token:
                description: API Token to be used for API calls.
                properties:
                  secretKeyRef:
                    description: Selects a key of a secret in the workspace's namespace
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - secretKeyRef
                type: object
            required:
            - name
            - organization
            - token
            type: object
          status:
            description: AgentPoolStatus defines the observed state of AgentPool
            properties:
              agentPoolID:
                description: Agent Pool ID that is managed by the controller.
                type: string
              agentTokens:
                description: List of the agent tokens generated by the controller.
                items:
                  description: 'Agent Token is a secret token that a Terraform Cloud
                    Agent is used to connect to the Terraform Cloud Agent Pool. More
                    infromation: - https://developer.hashicorp.com/terraform/cloud-docs/agents'
                  properties:
                    createdAt:
                      description: Timestamp of when the agent token was created.
                      format: int64
                      type: integer
                    id:
                      description: Agent Token ID.
                      type: string
                    lastUsedAt:
                      description: Timestamp of when the agent token was last used.
                      format: int64
                      type: integer
                    name:
                      description: Agent Token name.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              observedGeneration:
                description: Real world state generation.
                format: int64
                type: integer
            required:
            - agentPoolID
            - observedGeneration
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
