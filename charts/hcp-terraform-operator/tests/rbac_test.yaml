# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

suite: Create RBAC Roles and Bindings
templates:
  - role.yaml
  - rolebinding.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
release:
  name: this
  namespace: default
tests:
  - it: Should Create a Service
    asserts:
      - hasDocuments:
          count: 6
      - isKind:
          of: Role
